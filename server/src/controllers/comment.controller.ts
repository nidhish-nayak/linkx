import { type Request, type Response } from "express";

export const getComments = (_req: Request, _res: Response) => {};

export const addComment = (_req: Request, _res: Response) => {};

export const deleteComment = (_req: Request, _res: Response) => {};
