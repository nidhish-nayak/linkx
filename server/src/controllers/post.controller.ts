import { type Request, type Response } from "express";

export const getPosts = (_req: Request, _res: Response) => {};

export const addPost = (_req: Request, _res: Response) => {};

export const deletePost = (_req: Request, _res: Response) => {};
